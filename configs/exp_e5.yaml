experiment:
  name: e5              # ← e1에서 변경
  seed: 42
  repeats: 3
model:
  name: qwen2.5:3b
  endpoint: http://localhost:11434
registry:
  variant: core8
dataset:
  path: data/final/dataset_final.jsonl
output:
  base_dir: runs
  run_id: null
  logs_file: logs.jsonl
gate:
  enabled: true         # ← false에서 변경
scoring:
  synonyms_path: data/synonyms.yaml
  judge_clarify: false